apiVersion: v1
kind: Service
metadata:
  name: marilynvilcapuma-api-service
  namespace: marilynvilcapuma-api-ns
  labels:
    app: marilynvilcapuma-api
spec:
  selector:
    app: marilynvilcapuma-api
  type: NodePort
  ports:
    - port: 8085
      protocol: TCP
      targetPort: 8085
      nodePort: 30080
